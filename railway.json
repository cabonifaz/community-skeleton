{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p var/cache var/log && chmod -R 755 var/ public/ && php bin/console doctrine:migrations:migrate --no-interaction && php bin/console uvdesk:configure-helpdesk --env=prod && apache2-foreground"
  }
}