{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "chmod -R 755 var/ public/ && php bin/console cache:clear --env=prod && php bin/console uvdesk:configure-helpdesk --env=prod && ./configure-apache.sh && apache2-foreground"
  }
}