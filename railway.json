{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && php bin/console assets:install --symlink public && chmod +x start.sh"
  },
  "deploy": {
    "startCommand": "chmod +x start.sh && ./start.sh",
    "restartPolicyType": "ON_FAILURE"
  }
}