{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "php bin/console uvdesk:configure-helpdesk --env=prod --no-interaction || true && php -S 0.0.0.0:8080 -t public public/index.php"
  }
}