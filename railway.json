{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader && chmod +x auto-install.sh"
  },
  "deploy": {
    "startCommand": "./auto-install.sh"
  }
}